@model IEnumerable<CustomAltar>
@{ 
    ViewData["Title"] = "Custom Altars";
}

<h2>@ViewData["Title"]</h2>
<table class="table table-striped table-condensed">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    @foreach (var customAltar in Model)
    {
        <tr>
            <td>@customAltar.Id</td>
            <td>@customAltar.Name</td>
            <td>
            <a asp-controller="Solo" asp-action="Index" asp-route-customid="@customAltar.Id">Play</a> |
            <a asp-controller="Tools" asp-action="Anglemap" asp-route-customid="@customAltar.Id">Edit</a></td>
        </tr>
    }
</table>