enum ActionType { Idle, Move, Attract }

enum PathMode { Sight, Orb, Player }

enum OrbControlState { Free, Controlled, Wait1, Wait2 }

enum Altar {
    None,
    Air,
    Mind,
    Water,
    Earth,
    Fire,
    Body
}

enum Tile {
    Floor,
    Barrier,
    Rock,
    Water,
    WallW,
    WallS,
    WallSW,
    Minipillar1,
    Minipillar2
}
