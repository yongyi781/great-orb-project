///<reference path="../runeappslib.js"/>
///<reference path="../alt1lib.js"/>
"use strict";

ImageData.fromBase64(i => ChatBoxReader.plusbutton = i, "");
ImageData.fromBase64(i => ChatBoxReader.minusbutton = i, "");
ImageData.fromBase64(i => ChatBoxReader.filterbutton = i, "iVBORw0KGgoAAAANSUhEUgAAAA4AAAAPCAYAAADUFP50AAAAaElEQVQ4T9XLsQ2AMAxEUc+BxBysySTsQcEI7EF36JAcORdHoUMUv8jFz65jgXZuU2lf5zQD0OARonkg4+Eb6PcFMgc9GG8ryHpQ76qHp1D/WTOwCPXPS8fvINM/Lx1/CHWPpeMIAbAba/5W9dGOZAMAAAAASUVORK5CYII=");
ImageData.fromBase64(i => ChatBoxReader.chatbubble = i, "");
ImageData.fromBase64(i => ChatBoxReader.chatLegacyBorder = i, "iVBORw0KGgoAAAANSUhEUgAAAAQAAAAPCAIAAABMVPnqAAAAIUlEQVQYV2P4jwRAnNk92RA0mDjzJuRCOUAWlAMF//8DAJmOfBu3Ydq7AAAAAElFTkSuQmCC");
ImageData.fromBase64(i => ChatBoxReader.entertochat = i, "iVBORw0KGgoAAAANSUhEUgAAABgAAAAJCAYAAAAo/ezGAAAAdElEQVQ4T62RSw7AIAhEOQTH8WCsXbvmuhRsTdQipqYmz88MJI4CM0tDB/zNPelipJT06Bee4oo9UbqV1+uDscLS5Zx163ulFN2OOiLWVxnEE1YpGq74FSISu/GsGy/hhDBBixgVecw9YYL6GQ+9uWPfI3ABK/Blt6d3IYgAAAAASUVORK5CYII=");
ImageData.fromBase64(i => ChatBoxReader.gameoff = i, "iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAYAAAAf8/9hAAAAdUlEQVQ4T62MsQrAIAxE/bwOhS7tkMHBLi79/zVthAvaCjZq4MFx5J6jePEI8wTPOQsq2M/YJcAuBQHlH/LfuQILVQFyi0Kw+cCClMgt8t9CYEEFK3kVINfAEKBPI0FK5BoYAvSFwIIKloNYeD+0+Ah6GRQQ30BWZJSYN2VIAAAAAElFTkSuQmCC");
ImageData.fromBase64(i => ChatBoxReader.gamefilter = i, "");
ImageData.fromBase64(i => ChatBoxReader.gameall = i, "");
ImageData.fromBase64(i => ChatBoxReader.legacyreport = i, "");
ImageData.fromBase64(i => ChatBoxReader.reportbutton = i, "");

ChatBoxReader.timestampOffset = 56;
ChatBoxReader.defaultcolors = [
    [0, 255, 0],
    [0, 255, 255],
    [0, 175, 255],
    [0, 0, 255],
    [255, 82, 86],
    [159, 255, 159],
    [0, 111, 0],
    [255, 143, 143],
    [255, 152, 31],
    [255, 111, 0],
    [255, 255, 0],
    [239, 0, 0],
    [239, 0, 175],
    [255, 79, 255],
    [175, 127, 255],
    [48, 48, 48],
    [191, 191, 191],
    [127, 255, 255],
    [128, 0, 0],
    [255, 255, 255],
    [127, 169, 255],
    [255, 140, 56], //orange drop received text
    [255, 0, 0], //red achievement world message
    [69, 178, 71], //blueish green friend broadcast
    [164, 153, 125], //brownish gray friends/fc/cc list name
    [215, 195, 119] //interface preset color
];
function ChatBoxReader() {
    var me = this;
    this.pos = null;
    this.debug = null;
    this.overlaplines = [];
    this.readargs = { backwards: true };

    this.lineheight = 14;
    this.minoverlap = 2;
    this.diffRead = true;

    this.read = function (img) {
        var t = Date.now();
        if (!me.pos) { return null; }
        var box = me.pos.mainbox;
        if (!img) { img = a1lib.bindregion(box.rect.x + (box.leftfound ? 0 : -300), box.rect.y, box.rect.width, box.rect.height); }
        if (!img) { return null; }

        //add timestamp colors if needed
        if (box.timestamp && this.readargs.colors) {
            var cols = [a1lib.mixcolor(127, 169, 255), a1lib.mixcolor(255, 255, 255)];
            for (var a in cols) {
                if (this.readargs.colors.indexOf(cols[a]) == -1) { this.readargs.colors.push(cols[a]); }
            }
        }

        //TODO check scrollbar
        var imgline0y = box.line0y + box.rect.y - img.y;
        var imgline0x = box.line0x + box.rect.x - img.x;

        var readlines = [];
        var newlines = [];
        for (var line = 0; true; line++) {
            var liney = box.line0y - line * this.lineheight;
            var imgliney = liney + box.rect.y - img.y;
            if (liney - this.lineheight < 0) {
                newlines = readlines;
                break;
            }

            var str = jsonDecode(alt1.bindReadStringEx(img.handle, imgline0x, imgliney, jsonEncode(this.readargs)));
            //retry with offset if timestamps are enabled
            if (!str && box.timestamp) { str = jsonDecode(alt1.bindReadStringEx(img.handle, imgline0x + ChatBoxReader.timestampOffset, imgliney, jsonEncode(this.readargs))); }
            readlines.unshift(str ? str : { text: "", fragments: [] });
            //qw(str);

            //combine with previous reads
            if (this.diffRead) {
                if (readlines.length >= this.overlaplines.length && this.overlaplines.length >= this.minoverlap) {
                    var matched = true;
                    for (var a = 0; a < this.overlaplines.length; a++) {
                        if (!this.matchLines(this.overlaplines[a].text, readlines[a].text)) { matched = false; break; }
                    }
                    if (matched) {
                        newlines = readlines.slice(this.overlaplines.length, readlines.length);
                        break;
                    }
                }
            }
        }
        this.overlaplines = this.overlaplines.concat(newlines);
        if (this.overlaplines.length > this.minoverlap) { this.overlaplines.splice(0, this.overlaplines.length - this.minoverlap); }

        //qw("Read chat attempt time: " + (Date.now() - t));
        //for (var a = 0; a < newlines.length; a++) { qw(newlines[a]); }
        return newlines;
    }

    //convert some similar characters to prevent problems when a character is slightly misread
    this.simplefyLine = function (str) {
        str = str.replace(/[\[\]\.\':;,_ ]/g, "");
        str = str.replace(/[|!lIji]/g, "l");
        return str;
    }

    this.matchLines = function (line1, line2) {
        return this.simplefyLine(line1) == this.simplefyLine(line2);
    }

    this.checkLegacyBG = function (buf, x, y) {
        var p = buf.getPixel(x, y);
        return coldiff(p[0], p[1], p[2], 155, 140, 107) < 20;
    }

    this.find = function (img) {
        if (!img) { img = a1lib.bindfullrs(); }
        if (!img) { return null; }
        var toprights = [];
        a1lib.findsubimg(img, ChatBoxReader.plusbutton).forEach(loc => toprights.push({ x: loc.x + 2, y: loc.y - 1, type: "hidden" }));
        a1lib.findsubimg(img, ChatBoxReader.minusbutton).forEach(loc => toprights.push({ x: loc.x + 2, y: loc.y + 21, type: "full" }));
        a1lib.findsubimg(img, ChatBoxReader.filterbutton).forEach(loc => toprights.push({ x: loc.x + 18, y: loc.y, type: "small" }));
        //qw(toprights);

        var botlefts = [];
        a1lib.findsubimg(img, ChatBoxReader.chatbubble).forEach(loc => {
            //107,2 press enter to chat
            //102,2 click here to chat
            var data = img.toData(loc.x + 102, loc.y + 1, 28 + (107 - 102), 10);
            if (a1lib.simplecompare(data, ChatBoxReader.entertochat, 0, 1) !== false || a1lib.simplecompare(data, ChatBoxReader.entertochat, (107 - 102), 1) !== false) {
                botlefts.push(loc);
            }
            //i don't even know anymore some times the bubble is 1px higher (i think it might be java related)
            else if (a1lib.simplecompare(data, ChatBoxReader.entertochat, 0, 0) !== false || a1lib.simplecompare(data, ChatBoxReader.entertochat, (107 - 102), 0) !== false) {
                loc.y -= 1;
                botlefts.push(loc);
            }
            else {
                var pixel = img.toData(loc.x, loc.y - 2, 1, 1);
                if (pixel.data[0] == 255 && pixel.data[1] == 255 && pixel.data[2] == 255) {
                    botlefts.push(loc);
                }
                else {
                    //qw("unlinked quickchat bubble " + JSON.stringify(loc));
                }
            }
        });
        a1lib.findsubimg(img, ChatBoxReader.chatLegacyBorder).forEach(loc => {
            botlefts.push({ x: loc.x, y: loc.y - 1 });
        });
        //qw(botlefts);

        //check if we're in full-on legacy
        if (botlefts.length == 1 && toprights.length == 0) {
            //cheat in a topright without knowing it's actual height
            var pos = a1lib.findsubimg(img, ChatBoxReader.legacyreport);
            if (pos.length == 1) { toprights.push({ x: pos[0].x + 32, y: pos[0].y - 170, type: "legacy" }); }
        }

        var groups = [];
        var recurs = 0;
        var groupcorners = function () {
            recurs++;
            var done = true;
            for (var a in toprights) {
                if (groups.find(q => q.topright == toprights[a])) { continue; }
                done = false;
                for (var b in botlefts) {
                    if (groups.find(q => q.botleft == botlefts[b])) { continue; }
                    var group = {
                        timestamp: false,
                        type: "main",
                        leftfound: false,
                        topright: toprights[a],
                        botleft: botlefts[b],
                        rect: new Rect(botlefts[b].x, toprights[a].y, toprights[a].x - botlefts[b].x, botlefts[b].y - toprights[a].y)
                    };
                    if (groups.find(q => q.rect.overlaps(group.rect))) { continue; }
                    groups[groups.length] = group;
                    if (groupcorners()) { return true; }
                    groups.splice(groups.length - 1, 1);
                }
            }
            return done;
        }

        if (!groupcorners()) { return null; }
        //qw(recurs);
        //qw(groups);
        var mainbox = null;
        var readargs = JSON.stringify({ colors: [a1lib.mixcolor(255, 255, 255)], backwards: true });
        groups.forEach(group => {
            var nameread = jsonDecode(alt1.bindReadStringEx(img.handle, group.rect.x - 10, group.rect.y + group.rect.height + 9, readargs));
            if (nameread) {
                var d = 0;
                if (nameread.text == "Clan Chat") { group.type = "cc"; d = 62; }
                else if (nameread.text == "Friends Chat") { group.type = "fc"; d = 76; }
                else if (nameread.text == "Group Chat") { group.type = "gc"; d = 69; }
                else if (nameread.text == "Guest Clan Chat") { group.type = "gcc"; d = 98; }
                if (d != 0) {
                    group.rect.x -= d;
                    group.rect.width += d;
                    group.leftfound = true;
                }
            }

            if (!group.leftfound && group.topright.type == "full") {
                var pos = [];
                if (pos.length == 0) { pos = a1lib.findsubimg(img, ChatBoxReader.gameall, null, group.rect.x - 300, group.rect.y - 22, 310, 16); }
                if (pos.length == 0) { pos = a1lib.findsubimg(img, ChatBoxReader.gamefilter, null, group.rect.x - 300, group.rect.y - 22, 310, 16); }
                if (pos.length == 0) { pos = a1lib.findsubimg(img, ChatBoxReader.gameoff, null, group.rect.x - 300, group.rect.y - 22, 310, 16); }
                if (pos.length != 0) {
                    group.leftfound = true;
                    var d = group.rect.x - pos[0].x;
                    group.rect.x -= d;
                    group.rect.width += d;
                }
            }
            //alt1.overLayRect(a1lib.mixcolor(255, 255, 255), group.rect.x, group.rect.y, group.rect.width, group.rect.height, 10000, 2);
            //alt1.overLayTextEx(group.type, a1lib.mixcolor(255, 255, 255), 20, group.rect.x + group.rect.width / 2 | 0, group.rect.y + group.rect.height / 2 | 0, 10000, "", true, true);


            group.line0x = 0;
            group.line0y = group.rect.height - 10;//- 9;//-10 before mobile interface update

            if (group.leftfound) { group.timestamp = me.checkTimestamp(img, group); }
            if (mainbox == null || group.type == "main") { mainbox = group; }
        });

        if (groups.length == 0) { return null; }

        me.pos = {
            mainbox: mainbox,
            boxes: groups
        };
        return me.pos;
    }

    this.checkTimestamp = function (img, pos) {
        //the chatbox has timestamps if in the first 3 lines a line start with [00:00:00]
        var readargs = { colors: [a1lib.mixcolor(127, 169, 255)] };
        for (var line = 0; line < 3; line++) {
            var y = pos.rect.y + pos.line0y - line * this.lineheight;
            var x = pos.rect.x + pos.line0x;
            x += 3;//the leading '[' can't be the positioning char
            var str = jsonDecode(alt1.bindReadStringEx(img.handle, x, y, jsonEncode(readargs)));

            if (str && str.text.match(/^\d\d:\d\d:\d\d/)) { return true; }
        }
        return false;
    }
}
ChatBoxReader.getFontColor = function (buffer, x, y, w, h) {
    var bestscore = -Infinity;
    var bestx = 0, besty = 0;
    var data = buffer.data;

    for (var cx = x; cx < x + w - 1; cx++) {
        for (var cy = y; cy < y + h - 1; cy++) {
            var i1 = 4 * cx + 4 * buffer.width * cy;
            var i2 = 4 * (cx + 1) + 4 * buffer.width * (cy + 1);

            var colorness = data[i1] + data[i1 + 1] + data[i1 + 2];
            var blackness = data[i2] + data[i2 + 1] + data[i2 + 2];

            var score = Math.min(255, 255 + 20 - blackness) * colorness;
            if (score > bestscore) {
                bestscore = score;
                bestx = cx;
                besty = cy;
            }
        }
    }
    return buffer.getPixel(bestx, besty);
}
